<Window x:Class="league_mastery_overlay.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize">

    <Canvas x:Name="RootCanvas">
        <!-- DEBUG PLACEHOLDER -->
        <Border Width="200"
                Height="40"
                Background="#AA000000"
                CornerRadius="4"
                Canvas.Left="20"
                Canvas.Top="20">
            <TextBlock x:Name="DebugText"
                       Text="Overlay running"
                       Foreground="White"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"/>
        </Border>
    </Canvas>
</Window>
